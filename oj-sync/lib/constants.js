/** Success: green + check (U+2713). */
export const BADGE_OK = "\u2713";

/** Opens CP Helper in the editor only (no JSON in URI). Chrome may prompt once. */
export const DEFAULT_FOCUS_URI = "vscode://from-cero.cp-helper/focusSamples";

/** CP Helper listens here by default — no vscode:// tab, no Chrome prompt. */
export const DEFAULT_LOCAL_IMPORT_URL = "http://127.0.0.1:17337/import";
